# Python支持一种数据结构的基本概念，名为容器(container)。
# 容器基本上就是可包含其 他对象的对象。
# 两种主要的容器是序列(如列表和元组)和映射(如字典)。
# 在序列中， 每个元素都有编号，而在映射中，每个元素都有名称(也叫键)。
# 映射将在第4章详细讨 论。有一种既不是序列也不是映射的容器，它就是集合(set)，
# 列表是可修改的
edward = ['Edward',32]
john = ['john',40]
database = [edward,john]
print(database)